server: 
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id : ms-cuenta
          uri: lb://localhost:8001/cuentas
          preducates:
            - Path=/cuentas